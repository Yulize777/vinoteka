import React from "react";
import {Form} from "../../components/Form/Form";

export const Login = () => {
    return (
        <section className="login">
            <Form/>
        </section>
    )
}